Please have the following installed beforehand
- NPM/Node from https://nodejs.org/en/download/

Do the following in a terminal in the root folder of the project
```sh
npm install -g gatsby-cli
npm install
gatsby develop
```